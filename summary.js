/* 
1. Variable Declare (let, const) three types variable
1.2 condition (<,>, ===, !==, <=, >=)&& ||, if-else
1.3 array: declare, length, index, push, pop, indexOf
1.4 for loop, while
1.5 function return parameter
1.6 Object property, including array, object
---------------------------
ES6
1. template string ${}
2. spread (...) copy and array then add a element new array
2.1 use filter to remove an element from an array
3.1 Arrow no parameter  ()
3.2 single parameter
3.3 multiple paramerter ()
3.4 milti line
4. destructuring: object and array desturcturing
5. object shorthand
6. function  parameter defult value
7. Optional Chaining (?.)
-----------------
Browser API
1. local storage session storage
2. location Api
3. History Api
4. fetch
----------------
Others
1.Array: map, forEach, filter, find
2. ternary operator
3. logical an logical or &&||
4. JSON (stringfy, parse)
5. +, ''

*/
const x = 50;
const y = 100;
const obj ={
    x:x,
    y:y
}

const name = 'solimullah'
const area = 'dhaka'

const nobab = {
    name: name,
    area: area
}

const nobbab = {name, area}